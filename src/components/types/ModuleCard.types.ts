type ModuleCardProps = {
    id: string;
    name: string;
    description: string;
    subtitle?: string; // Nuevo campo
    icon: string;
    href: string;
    color?: string;
}

export type { ModuleCardProps };
